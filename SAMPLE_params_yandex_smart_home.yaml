filter:
  include_entities:
    - vacuum.neatsvor_<device_id>_vacuum
    - sensor.neatsvor_<device_id>_filter
    - sensor.neatsvor_<device_id>_main_brush
    - sensor.neatsvor_<device_id>_side_brush
    - sensor.neatsvor_<device_id>_total_clean_area
    - sensor.neatsvor_<device_id>_total_clean_time
    - sensor.neatsvor_<device_id>_last_clean_area
    - sensor.neatsvor_<device_id>_last_clean_duration

entity_config:
#====== neatsvor_<device_id>_vacuum ==========

  vacuum.neatsvor_<device_id>_vacuum:
    name: Пылесос Neatsvor
    properties:
      - type: battery_level
        entity: vacuum.neatsvor_<device_id>_vacuum
        attribute: battery_level

  sensor.neatsvor_<device_id>_filter:
    name: HEPA фильтр
    properties:
      - type: float.meter
        entity: sensor.neatsvor_<device_id>_filter

  sensor.neatsvor_<device_id>_main_brush:
    name: Основная щётка
    properties:
      - type: float.meter
        entity: sensor.neatsvor_<device_id>_main_brush

  sensor.neatsvor_<device_id>_side_brush:
    name: Боковая щётка
    properties:
      - type: float.meter
        entity: sensor.neatsvor_<device_id>_side_brush

  sensor.neatsvor_<device_id>_total_clean_area:
    name: Общая площадь уборки
    properties:
      - type: float.meter
        entity: sensor.neatsvor_<device_id>_total_clean_area

  sensor.neatsvor_<device_id>_total_clean_time:
    name: Общее время уборки
    properties:
      - type: float.meter
        entity: sensor.neatsvor_<device_id>_total_clean_time

  sensor.neatsvor_<device_id>_last_clean_area:
    name: Площадь последней уборки
    properties:
      - type: float.meter
        entity: sensor.neatsvor_<device_id>_last_clean_area

  sensor.neatsvor_<device_id>_last_clean_duration:
    name: Время последней уборки
    properties:
      - type: float.meter
        entity: sensor.neatsvor_<device_id>_last_clean_duration

#====== neatsvor_<device_id>_vacuum ==========
